検査コミット：`3a80f04538091db5286a79adf1f1b007e9cb1df6`

ブランチ：`main`／正本：`SPEC.md` v2.4.1・`CLAUDE.md` v1.2／検査日：2026-09-19

## 指摘（重大度・箇所・根拠）

Codex③の主要な通信・保持・監査の修正は、今回再現した範囲では維持されている。ただし、文書の残存、新たな入力境界、変換記録の不一致がある。「反映時の19件」には個別一覧がなく、19件すべての対応を一意に確認できたとは記録しない。

検査方法：既存Reader試験は **289 passed、1 deselected**。除外したR01は、バイトコードを書かない `-B` を明示した別プロセス3条件で14呼び出しの完全一致を確認した。通信・配置・監査は現物の関数／クラスをASTから抽出したメモリ試験と依存コードの照合。SIGTERMは実際のハンドラ・固定版Gradioの待機処理と擬似サーバを使った自分の子プロセスで確認した。実HTTPの全試験、Dockerのビルド・起動、SpacesのS04・P01〜P03は今回再実施していない。変更は本報告書だけで、コミットしていない。

### 新規・残存事項

**F1 — P1（公開前の要約条件が残存）：新設文書に上流不具合の詳細が戻っている。**

- 箇所：`docs/spaces_facts.md:36`、同101行。根拠：`SPEC.md:175`。
- Codex③-9で旧説明を要約した一方、新しいSpaces調査メモには、具体的な上流ファイル・行番号・不一致条件・内部の分岐を組み合わせた詳細が残る。報告済み、または詳細公開を承認したという記録は見つからない。公開用の互換性説明への要約が必要。
- README・DECISIONS・docsの既存58文書を走査した範囲では、旧指摘の利用者固有の絶対パスと非公開台帳IDは除去済み。鍵らしい値・秘密鍵ブロックは検出しなかった。これは文字列走査の結果であり、秘密情報が存在しないことの保証ではない。

**F2 — P2：Spacesのツール名に接頭辞が付くという旧記述が残る。**

- 箇所：`README.md:300`。根拠：`app.py:44`、`app.py:70`、`SPEC.md:159`。
- 現実装はimport前に `SYSTEM`・`SPACE_ID` を除去するため、spacesでもツール名に接頭辞を付けない。READMEの227・355行はこの現実装に合っているが、300行だけが逆の説明になっている。利用者が呼び出す名前を誤る。

**F3 — P2：設定入りHTMLの公開範囲をloopback内に限定して説明している。**

- 箇所：`README.md:265`。根拠：`app.py:750`。
- spacesで許可していないHostの `/` は固定の健康検査HTMLになるが、許可した公開Hostの `GET /` は従来のGradioへ渡る。現物Guardの試験でも、許可外Hostは固定HTML・後段呼出しなし、許可Hostは後段への転送を確認した。「設定はloopback内で引き続き出ている」では公開モードの情報公開範囲を過小に示す。

**F4 — P3：長い数字のHostポートが400拒否にならず、例外になる。**

- 箇所：`app.py:287`〜290。根拠：`docs/rules/LIMITS.md:32`。
- ポート文字列を長さ制限なしで `int()` に渡す。数字5,000桁のポートを含むHostは固定版h11がRequestとして受理するが、現物 `_host_name()` は整数変換で `ValueError` を送出する。Guardの400拒否まで到達しない。実HTTPでの応答コードは今回再測定していない。

**F5 — P3：ゼロ幅文字を含む空白の変換記録がNORM規則7と一致しない。**

- 箇所：`mekiki_reader/normalize.py:174`。根拠：`docs/rules/NORM.md:19`。
- 規則7は空白の並びにゼロ幅文字がある場合、`WS-COLLAPSE` と `WS-ZW` の両方を記録すると定める。しかし空白部分が通常の空白一字なら前者を省略する。
- 実データで `AI \u200bcan assist play. It cannot take one's place in it.` を照合すると `ok / normalized` だが、`normalization_applied` と `payload.diffs[0].rules` は `WS-ZW` だけになる。本文・位置の照合は正しく、変換記録に限定された不一致。強調記号を挟む空白の修正は維持されている。

**F6 — P3：回収中セッションの保持規則に旧記述が残る。**

- 箇所：`docs/rules/LIMITS.md:36`。根拠：`app.py:514`〜517。
- 「取りに来ているセッションには手を付けず」に対し、実装は回収中でも件数・容量上限を適用し、期限だけを免除する。現物の掃除処理で回収中67件が64件に減ることを確認した。これは修正の意図と `README.md:356` に合っているため、LIMITS側を直す。

**F7 — P3：spacesのポート契約が正本内で一意に読めない。**

- 箇所：`SPEC.md:46`、同48行。関連：`app.py:1138`、`DECISIONS.md:280`、`docs/rules/LIMITS.md:44`。
- 配置モードの段落はbindを `0.0.0.0:7860` とする一方、その前段はモードを限定せず `MEKIKI_READER_PORT` による変更を認める。現実装・判断ログ・README・LIMITSはspacesでも可変という解釈で一致する。
- 現物関数でspaces設定とポート18123が同時に受理されることを確認した。既定7860はDockerのEXPOSE・READMEのYAMLと一致しており、通常配置が失敗するという指摘ではない。変更時に転送先とずれる可能性があるため、固定か可変かを正本で確定する必要がある。

**F8 — P3：「反映時19件」の個別対応を追跡できない。**

- 箇所：`DECISIONS.md:273`。
- リポジトリ内の該当記録は一段落だけで、複数の修正がまとめて列記されている。19項目それぞれの識別番号・箇所・試験を一意に復元できない。以下では記録から識別できる論点を分けて検算した。19件という総数を埋めるために未記録の項目を推定していない。

**F9 — P3：現状の版・検収・依存動作に関する記述が一部古い。**

| 箇所 | 現物との違い |
|---|---|
| `SPEC.md:140` | 現行の上限規則を `LIMITS-2.0.0` と記すが、実装・規則文書・応答RULESは3.1.0 |
| `SPEC.md:94`、同160行 | 応答13本・E01残り8本予定という記述。検収総括と判断ログはE01 20本＋E02 1本が完了 |
| `README.md:348` | filelockの一時ファイルを作業ディレクトリへ作ると記すが、固定依存の `filelock/_strict.py:70` は `TemporaryDirectory()` 内に作る |
| `README.md:181` | Linux検証の除外3項目をWindows専用と記すが、`requirements.txt:1291` のtzdataはemscriptenも対象。判断ログの説明は正しい |

### Codex③の11件との対応

原本：`docs/review/codex-3-report.md`。反映記録：`DECISIONS.md:255`〜273。

| ③項目 | 今回の結果・根拠 |
|---|---|
| 1 本文受信・POST応答の受付上限 | 修正維持。`app.py:161,820,833`。未完本文130本のメモリ試験で受付32・待機96・503が2本、切断後は0／0。送信停止の模擬試験は期限でabortを呼び枠を解放 |
| 2 LRUから追い出したIDの残存 | 修正維持。`app.py:420,435`。2,100セッション投入後、結果・ID・状態・時刻の表がいずれも2,000。上流delに伴う解放も確認 |
| 3 120秒で正常セッションを消す | 修正維持。`app.py:473,481`。空セッションと処理中セッションは121秒後も残り、期限超過結果のみ破棄。回収中の期限免除も確認。文書の残存はF6 |
| 4 UTF-8保持量・応答容量の過大主張 | 修正維持。`app.py:401`。日本語を含む4,500,011バイトのメッセージを破棄。1655字反復入力で100結果・208,887バイトを再現し、README／LIMITSの約204 KiBという「測定例」と一致 |
| 5 起動中の内部クライアント競合 | 準備前の入口を修正。`app.py:785`。MCP本体・末尾slashなし・schemaの3経路が503。`mark_ready()` は内部クライアント生成と起動後検査の後。実クライアント100本同時試験は今回再実施していない |
| 6 S03の相対パス・FD解決 | 旧症状は解消。`tests/_support/server_launcher.py:98,112,141,197,243`。macOSの読取FDを解決し、相対open・FD宛rename・dir_fd付きopen・無効FDを実行前に遮断。絶対パスでの記録を確認 |
| 7 標準エラーへの入力残存 | 旧出力関数の症状は解消。`app.py:313,322,341,375`。例外本文と完成済みログ文字列に入れた検査用マーカーは出力0件。待受前にログ出口を変更する順序も確認。全HTTP入力・Spaces実ログは今回再測定していない |
| 8 Spaces構成・復帰検収 | 配置モード・環境除去・pwa固定・Docker構成が追加された。詳細は下表。Docker実行・S04・P01〜P03が未実施という記録を完了扱いにしていない。接頭辞説明にF2、ポート契約にF7 |
| 9 公開前の削除・要約 | 旧絶対パス・非公開参照は除去済み。新設文書の残存はF1 |
| 10 NORMの空白右側の強調記号 | 旧症状は解消。`mekiki_reader/normalize.py:164` も `significant()` で探索。左右・先頭末尾の差分を確認。基本5文字種、長さ1〜6の19,530組で強調記号除去との正規化本文の等価性を検算。別の変換記録の境界はF5 |
| 11 版規則の適用開始点 | 修正維持。`docs/rules/README.md:5` とSPECはともに `main` の `72b13eb` 以降。E01等の進捗記述はF9 |

### 「反映時19件」の段落から識別できる論点

次表は原記録の論点を分解したもので、原記録に存在しない19個の識別番号を新設するものではない。

| 記録された論点 | 確認結果・確認方法 |
|---|---|
| 期限後も接続が閉じない | `app.py:387,845`。送信停止のメモリ試験でabort呼出し・枠解放。実TCPは未再測定 |
| 拒否接続が本文を保持する | `app.py:688,710`。64 KiB・1秒のdrainとConnection: closeをコード確認。実接続回収は未再測定 |
| 回収中セッションに上限がない | `app.py:514`。メモリ試験で67件→64件。文書はF6 |
| 上流のdelで関連状態が残る | `app.py:457`。ID・状態・時刻を解放。実行イベントの後処理は上流に残すことをメモリ確認 |
| 捨てた完了結果のIDが残る | `app.py:523`。完了済みIDと空集合エントリを削除することをメモリ確認 |
| f文字列ログに入力が埋め込まれる | `app.py:348`。完成済みログ文字列の検査用マーカーも出力しないことをメモリ確認 |
| 待受直後のログ | `app.py:375,381`。uvicornのログ設定直後・待受前に出口を差し替える順序をコード確認 |
| 長時間経路の本文 | `app.py:791`。MCP GET・heartbeat・queue/dataの本文付き要求を400、後段呼出し0とするメモリ試験 |
| 監査の二重記録 | `tests/_support/server_launcher.py:207,249`。包み内のフラグで元open監査を抑える実装をコード確認 |
| 別名のパス | 相対パス・FD宛先はメモリ試験で解決。`tests/_support/server_launcher.py:154` のrealpath・dev/inode照合はコード確認。ハードリンク作成試験はしていない |
| supports_dir_fd | `tests/_support/server_launcher.py:259`。元関数の対応を包みに引き継ぐ処理をコード確認 |
| 陽性対照の安全 | `tests/_support/server_launcher.py:339,350`。rename元は存在しないファイル、openは非作成モード。対照一式は実行していない |
| NORMのゼロ幅 | 強調記号を挟む畳み込みは修正維持。通常空白一字＋ゼロ幅の変換記録はF5 |
| NORMの差分 | 先頭末尾のMARK-EMPH付き差分を既存試験と追加検算で確認 |
| 記述の食い違い | 全解消ではない。F2・F3・F6・F9 |
| 接続済みsendmsgの誤判定 | `tests/_support/server_launcher.py:63`。受信接続は自分の待受先、その他はpeer先で判定することをメモリ確認。外向きpeerを自己接続として免除しない |

### 配置モード・Docker・規則の確認

| 対象 | 確認結果 |
|---|---|
| local／spacesの差 | `read_deploy()` の差はbind・許可Host・健康検査の例外。その他の受付・保持・CORS・Reader処理は共通。ポートの仕様解釈はF7 |
| SPACE_HOST | spacesだけで読む。カンマ分割、前後空白除去、小文字化、DNS形式と各値253文字以下を検査、順序を保って重複除去。未指定・URL形・ポート付きの値は設定エラー。起動設定へ保存してからsanitizeする。環境のSPACE_HOST自体は残す仕様・試験と一致 |
| 環境除去 | 両モードで `GRADIO_*`、大文字／小文字のHTTP_PROXY・HTTPS_PROXY・ALL_PROXY・FTP_PROXY・NO_PROXY、およびSYSTEM・SPACE_ID・SPACE_AUTHOR_NAME・SPACE_REPO_NAME・SPACES_ZERO_GPU・OAUTH_*・HF_TOKEN・WEB_CONCURRENCY・FORWARDED_ALLOW_IPSを除去。GRADIO_ANALYTICS_ENABLED=False、HFの四固定値、NO_PROXY／no_proxyを再設定。現物関数と作り物の環境辞書で確認 |
| `/` のHost例外 | spaces・許可外Host・厳密に `/`・GET／HEADだけで固定HTML／空のHEAD本文を返す。POST、`//`、info、MCPへ例外は広がらない。Origin付きもCORS許可なし。localの許可外Hostは `/` でも400。許可Hostは従来のGradioへ渡る（F3） |
| 起動前後の確認 | `pwa=False`、環境除去、待受名・local_url、queue・ログ・abort・結果保持の差し替え、check_cors、内部クライアントを確認後に準備完了とするコード順序を確認 |
| SIGTERM | 自分の子プロセスで実ハンドラと固定版Gradioのblock_threadを実行し、SIGTERM→server.close呼出し→終了コード0を確認。擬似サーバであり、実接続を持つコンテナの停止や起動途中の停止は未検証。ハンドラ登録は準備完了後。DockerはSTOPSIGNAL SIGINTも指定 |
| Docker固定・依存 | Pythonタグ＋64桁ダイジェスト、`--require-hashes --only-binary=:all:`、UID1000、EXPOSE 7860、CMDの直接起動、bytecode書込み抑止を静的確認。ダイジェストは判断ログと一致。レジストリとの独立照合・ビルドは未実施。runtime66項目・dev6項目はexact pinとSHA-256付き、spacesパッケージは依存にない |
| Dockerの読み取り専用 | COPYはroot所有のコード・データを置く構成。現物対象にsymlink・group/other writableはない。実イメージのUIDと権限は未検証。親ディレクトリが実行UID所有なので、root所有だけでディレクトリ全体の置換禁止まで強制する構成ではない。任意書込み入口や原文改変を実証したという意味ではない |
| .dockerignore | .DS_Store・bytecode・.venv・.git・tests・docs等を除外。明示COPYはrequirements.txt・app.py・mekiki_reader/・data/だけ。現物の対象はReader8ファイルと許可データ18本＋bundle。既存の不要物の混入は確認しなかった |
| localの維持 | bindは127.0.0.1、Hostは従来のloopback集合、ポート既定7860／可変、`/` のHost検査あり。配置によるReader／promptの切替はない。NORM・LIMITSの版表示は承認された改版に追随 |
| 規則・表 | NORM-1.2.0・LIMITS-3.1.0の実装／規則文書／応答RULESが一致。TERMS・PATTERNS・NORMの再計算ハッシュも一致。生成器の語形追加をメモリ上だけで生成し、定数・生成文書・現行整合試験の一致を確認 |

再現性試験の出力SHA-256：`a253f12780c5f52b0179fefce655106e4aa626ef1d3964fbf92fbd8fe462e169`。
NORM表SHA-256：`19f90a79f945e17050264999caf4cd06dbe1056af0272ebfe079a9e10b16f6f0`。

## 差分案

| 対応 | 変更案・追加試験 |
|---|---|
| F1 | spaces_factsの該当説明を、採用版・必要な回避策・確認範囲の公開用説明へ要約する。未報告の詳細の公開可否は著者判断として記録する |
| F2 | README300を「spacesでも接頭辞なし」へ統一。ツール名・prompt名の説明をM01の期待値と照合する |
| F3 | `/` の設定公開をlocalとspacesで分けて明記。固定HTMLになる条件が「許可外HostのGET／HEAD」であることも同じ箇所に書く |
| F4 | Hostポートを整数化する前に桁数／形式を制限し、異常値は通常のHost拒否へ流す。IPv4・括弧付きIPv6・公開DNS名で長い数字のポートを追加検査。spacesの健康検査例外も例外送出で壊れないことを確認する |
| F5 | 規則7を維持するなら、ゼロ幅文字を含む空白並びにWS-COLLAPSEも記録する。通常空白一字＋各ゼロ幅文字について本文・normalization_applied・diffs.rulesを検査する |
| F6 | LIMITS36を「回収中は期限のみ免除。件数・容量制限は適用」に修正する |
| F7 | spacesで7860固定か、可変で転送先と同期させるかを正本で確定する。前者なら本番mainで固定／異なる値を拒否し、試験用ポートは起動器の内部引数で扱う。後者ならSPECの表現とREADME YAML変更条件をそろえる |
| F8 | 19件の元の検算記録から、各項目の識別番号・症状・修正箇所・試験を一行ずつ対応づける。現存段落から推定した19行を作らない |
| F9 | SPECの現行LIMITS版と検収本数を同期する。READMEの一時書込み場所、Linux検証で除外するプラットフォームの説明を訂正する。E01を追加実施する必要はない |
| 配置の残検証 | private SpaceでSPEC所定のS04・M01〜M03・P01・P02を実施し、実ポート・外側Host/CORS・ログ・再接続を確認する。SIGTERM／SIGINTは起動途中・アイドル・処理中の停止を区別して検査する。ファイル権限で置換禁止まで強制する場合は、実行UIDが書けない親配下へ作業ディレクトリを置いて実イメージで検証する。P03は正本の順序どおりpublic後に行う |
