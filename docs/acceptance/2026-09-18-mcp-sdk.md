# 検収記録：mcp SDK（Python 1.30.0） / 2026-09-18（再取得）

施工段階3の M01〜M03 に対応する記録。**Codex① の反映後（英語 prompts 6件・PATTERNS-0.1.1）に取り直した。**
初回（prompts 3件・PATTERNS-0.1.0 のとき）の記録は差し替えてある。**モデルを介さない機械的な確認**なので、E01・E02（対話のスモーク試験）はここでは行わない。
クライアント（Claude Code・Claude Desktop）の記録は別ファイルに作る。

## 1. 条件

| 項目 | 値 |
|---|---|
| 実施日時 | 2026-09-18 |
| 実施者 | 施工（Claude Code） |
| クライアント | `mcp` 1.30.0（Python SDK・`streamablehttp_client`） |
| モデル | なし（SDK から直接呼んだ） |
| 接続方式 | Streamable HTTP 直結 |
| 接続 URL（実測） | `http://127.0.0.1:7904/gradio_api/mcp/`（起動表示の `* Streamable HTTP URL:` と一致。既定は 7860） |
| サーバの起動表示 | `corpus 3.5.0 (6748061)・bundle 40a09c5ba422…` / `tools 7・resources 12・prompts 6（PROMPTS-0.1.0）` / `消した環境変数：なし` |
| `initialize` の応答 | `serverInfo: Mekiki Reader 1.30.0`（version は SDK の版が入る） |
| サーバの版 | ブランチ `codex1`（Codex① 反映後）・Python 3.13.15・gradio 6.27.0・`pytest -q` → 392 passed |
| 規則の版 | TERMS-0.1.1（30項目・表 `790e1240…`）・PATTERNS-0.1.1（49件・表 `92666827…`）・PROMPTS-0.1.0（6件）・NORM-1.0.0・SEARCH-1.0.0・LIMITS-1.0.0 |

## 2. 発見と呼び出し

| 項目 | 期待 | 実測 | 備考 |
|---|---|---|---|
| `tools/list` | 7件（接頭辞なし） | 7件・`list_papers` `get_section` `search_passages` `get_claim_record` `verify_quote` `check_compressions` `get_reading_guide` | 順序も登録順どおり |
| `list_papers` | `ok`・5本 | `ok`・results 5 | |
| `get_section`（ja・`t4-2-3`） | `ok` | `ok`・results 1 | |
| `get_section`（`language="en"`・`paper-t4`） | `ok`・`source_kind=translation` | `ok`・results 13 | 訳注・未訳 unit が別結果として並ぶ |
| `search_passages`（`answerability`・k=3） | `ok`・順位つき | `ok`・results 3 | |
| `get_claim_record`（`T5-N3`） | `ok`・`status` を逐語 | `ok`・results 1 | |
| `get_claim_record`（`T1-A1`） | `ledger_not_available` | `ledger_not_available`・candidates 40（T1 の全節） | ラベルを作らない |
| `verify_quote`（凍結文②） | `ok` | `ok`・results 1 | |
| `check_compressions`（承認済み語形を含む文） | `ok` | `ok`・results 5 | 関連原文つき |
| `get_reading_guide`（`modes`） | `ok` | `ok`・results 1 | |
| `resources/list` | 12件・テンプレート0件 | 12件・templates 0件 | |
| `resources/read` | 本文の SHA-256 が bundle と一致 | **12/12 一致** | |
| `prompts/list` | 6件（日本語3・英語3） | 6件 | |
| `prompts/get`（既知） | 文面が `mekiki_reader/prompts.py` と一致 | **6/6 一致** | |
| `prompts/get`（未知の名前） | 雛形を返さずエラー | `McpError: 'data'` | 番兵が止めた。雛形の文面は返っていない |

## 3. 不正な入力とゼロ件

| 入力 | 期待 | 実測 |
|---|---|---|
| `search_passages(k=0)` | `invalid_input`（通信は切れない） | `isError=False`・`status=invalid_input` |
| `search_passages(k="abc")` | スキーマ層のエラー | `isError=True`・`Input validation error: 'abc' is not of type 'integer'` |
| `search_passages(no_such_arg=1)` | 引数構築層のエラー | `isError=True`・`Parameter \`no_such_arg\` is not a valid key-word argument.…`（`tests/test_mcp.py::test_m03_schema_layer_violation`） |
| `get_section(anchor="t4-9")` | `unknown_id` と近傍候補 | `isError=False`・`status=unknown_id` |
| `get_section(paper_id="../../.env")` | `invalid_input`（応答にその文字列が出ない） | `isError=False`・`status=invalid_input`（文字列は応答に出ない） |
| `search_passages("zzqxjvw")` | `no_lexical_match` | `isError=False`・`status=no_lexical_match` |
| 直後の `list_papers` | `ok`（同じセッションで続けられる） | `ok` |

エラー文に内部の情報（パス・traceback・`site-packages`）が混ざらないことも確認した（`tests/test_mcp.py`）。

## 4. E01（スモーク・五問）

**未実施**。モデルを介する試験なので、SDK からの機械的な呼び出しでは行わない。Claude Code・Claude Desktop の記録で行う。

## 5. E02（事例と命令文）

**未実施**（同上）。ただし機械的な側面（命令風テキストを入れても状態・欄の構成・出典の範囲が変わらないこと）は
`tests/test_safety.py::test_s02_instruction_like_text_is_data` で確認済み（`tests/fixtures/instruction_like.json` の6件×4ツール）。

## 6. 未対応・備考

- `mcp-remote@0.14.2`（Node v24.13.1）を stdio で挟んだ経路でも、tools 7件・resources 12件・**prompts 6件**・`list_papers` の `ok`・`four_modes_en` の文面を確認した（英語版を載せた後に取り直した。Claude Desktop 用の橋渡しの動作確認）。
- 同時実行は上限4。8本同時に送っても、この試験では上限に達しなかった（関数内の時間が往復より短いため）。上限を1に下げた実測では8本中3本が通信層のエラーで返った。
- 自己呼び出しの経路（`/gradio_api/queue/join`）は順番待ちにしてある。`resources/read` を同時24本送っても 24/24 が `ok`（503 は0件）。
- 未知の prompt 名のエラー文に名前を入れることは、上流の実装の作りからできない（`DECISIONS.md` の「番兵の例外文」の行）。
