- 検査コミット：`40b2777451abbfc33d87c388c5625906b15d265c`
  ブランチ：`main`／正本：SPEC v2.3.1・CLAUDE v1.2

  ## 指摘（重大度・箇所・根拠）

  **Codex②の「すべて解消」という記録は維持できません。** [原本](docs/review/codex-2-report.md)・[対応表 (line 28)](docs/review/codex-2.md:28)を現物と照合した結果は次のとおりです。

  | 前回の項目             | 再検算結果                                                   |
  | ---------------------- | ------------------------------------------------------------ |
  | ②-1 queue・未回収結果  | 受付8・待機64・queue72は実装済み。保持処理に残存・退行あり〔下記2〜4〕 |
  | ②-2 LRU競合            | 修正維持。`lru_cache(maxsize=512)`・8スレッド試験成功        |
  | ②-3 受信期限           | 修正維持。チャンク共通の締切で408になることを確認            |
  | ②-4 S03監査            | 宛先判定・停止後保存・陽性対照は改善。パス検出に残存あり〔6〕 |
  | ②-5 IPv6 Host接尾辞    | 修正維持。`[::1]x80`を拒否                                   |
  | ②-6 生成器・表ハッシュ | 修正維持。語形追加をメモリ上で生成し、定数・再計算値・文書の一致を確認 |
  | ②-7 README等の過大記述 | 旧指摘は是正。新たな上限値の過大記述あり〔4〕                |
  | 残存④ queue            | 完全解消ではない〔2〜4〕                                     |
  | 残存⑥ S03              | 完全解消ではない〔6〕                                        |
  | 残存⑩ 規則文書         | 旧SEARCH・PROMPTS記述は是正。別の不一致あり〔10・11〕        |

  検証は七ツールの既存試験 **280件成功**、R01は書き込みを避けた別プロセス3条件で一致。通信層は現物コードを抽出したASGI試験、生成器は書き込みをメモリに置換して検査しました。実HTTPの全試験・Spaces検収は再実行していません。ファイル変更・コミットはありません。

  1. **P2：本文受信中とPOST応答送信中には、接続数の総上限がありません。**
     [app.py (line 423)](app.py:423)の本文先読みは受付制限より前です。未完本文128本で**8,388,480バイト保持・queue待機0・stream計数0・拒否0**を再現しました。さらに[300行 (line 300)](app.py:300)はMCPのGETだけを数えます。送信停止を模擬するとPOST応答40本が残り、実行4枠は全て空きました。8／8／32と各要求10秒だけでは、匿名利用者による資源消費を制限できません。
  2. **P2：LRUから追い出されたセッションのイベントIDが残り続けます。**
     [app.py (line 228)](app.py:228)は結果キューに残るセッションだけを掃除します。固定GradioのLRUへ2001セッションを作り、期限後に掃除すると、結果キューは0件でも、追い出されたセッションの`pending_event_ids_session`が残りました。セッション名を変える反復呼び出しで蓄積できます。
  3. **P2：120秒の掃除が正常な処理中セッションも削除します。**
     [app.py (line 229)](app.py:229)の期限は未回収結果の経過時間ではなく、セッションを最初に見た時刻です。空キュー・処理中IDを持つ正常セッションを121秒後に削除し、その後の現物`Queue.send_message()`が`KeyError`になることを確認しました。継続利用中のresources／promptsも失敗し得ます。
  4. **P2：4 MiB制限とREADMEの最悪応答量が成立しません。**
     [app.py (line 232)](app.py:232)はバイト数ではなく`len(repr(item))`を数えます。正規の応答を入れた実際のメッセージ21件は、直列化後**4,655,438バイト**でも判定値3,921,740で、破棄されませんでした。
     また[README:81 (line 81)](README.md:81)・[LIMITS:22 (line 22)](docs/rules/LIMITS.md:22)の「最悪約184 KiB」は、1655字の反復語形入力で**100結果・208,887バイト、約204 KiB**になります。100結果という上限と、応答容量の測定例を区別する必要があります。
  5. **P2：内部クライアントの単一化に起動中の競合が残ります。**
     [app.py (line 705)](app.py:705)で受付開始後、724行で内部クライアントを作ります。その間の外部要求を止める準備状態がありません。現物の生成関数を競合させ、**生成2個・登録1個・残存1個**を再現しました。既存試験は`READY`後に要求するため、この時間帯を検査しません。残存クライアントのheartbeatは内部例外と認識されず、再試行問題につながります。
  6. **P2：S03は相対openとディレクトリFD経由の操作を取りこぼします。**
     [server_launcher.py (line 136)](tests/_support/server_launcher.py:136)はopenの相対パスをそのまま保存するため、`data/...`への書き込み記録が[絶対パス前提の検査 (line 736)](tests/test_safety.py:736)を通ります。また[96行 (line 96)](tests/_support/server_launcher.py:96)の`/dev/fd`解決は、このmacOSでは`EINVAL`となり、dataのFDを宛先にした模擬renameイベントを遮断できませんでした。**監査の検出漏れであり、本番の原文書き換えを実証したものではありません。**
  7. **P2：利用者入力が標準エラーへ残る具体的な経路があります。**
     開放されたqueue経路で引数不足の呼び出しを送ると、Gradioの入力検証例外に受信した値が含まれ、[例外出力処理 (line 899)](.venv/lib/python3.13/site-packages/gradio/queueing.py:899)が印字します。現物の検証関数と例外出力で入力マーカーの記録を確認しました。`show_error=False`では防げません。[README:290 (line 290)](README.md:290)のログ開示を未実測のまま公開できません。
  8. **P1・公開条件未達：Spaces向け構成と復帰検収がありません。**
     [app.py (line 57)](app.py:57)はloopback固定、[101行 (line 101)](app.py:101)は公開Host名を許可しません。Spacesの転送先・Host書き換えを含む接続確認は未実施です。さらに`SYSTEM`・`SPACE_ID`は残るため、Spacesでは同梱Gradioの[別のリロード処理 (line 152)](.venv/lib/python3.13/site-packages/gradio/http_server.py:152)が動きます。ローカルS03だけで追加待受・通信・書き込みを判断できません。P01の再起動・休止復帰も未検収です。
  9. **P1・公開条件未達：削除・要約対象の記述が残っています。**
     非公開台帳参照は[DECISIONS:21 (line 21)](DECISIONS.md:21)・125行、[PLAN:218 (line 218)](docs/PLAN.md:218)、[stage0_answers:71 (line 71)](docs/stage0_answers.md:71)に残存。ローカルユーザー名入り絶対パスは[DECISIONS:196 (line 196)](DECISIONS.md:196)に残っています。上流不具合の再現詳細もDECISIONS 94・96・218行、README 223・235行、LIMITS 36・37行に残っています。[SPECの公開前条件 (line 171)](SPEC.md:171)に未達です。鍵らしい値・秘密鍵ブロックは今回の文字列検査では検出しませんでした。
  10. **P2：強調記号と空白の組合せでNORM契約に違反します。**
      [normalize.py (line 153)](mekiki_reader/normalize.py:153)は空白の右側にある強調記号を飛ばさず、[NORM:20 (line 20)](docs/rules/NORM.md:20)と不一致です。T4の同じ原文について、冒頭を`従業員が **遭遇**から…`とすると不一致、`従業員が** 遭遇**から…`ならnormalized一致になります。
  11. **P3：版規則の適用開始点が正本と違います。**
      [規則一覧:5 (line 5)](docs/rules/README.md:5)は日付基準、SPECは`72b13eb`以降というコミット基準です。なおE01残り8本は完了記録があり、不足ではありません。SPEC側の進捗記述が古い状態です。

  開放8区分（完全一致9パス＋heartbeatの前方一致）、指定された閉鎖経路、prompts 6件、三層の保証、改竄耐性を主張しない記述は一致しています。TE／CL・64 KiB境界・Host・孤立サロゲート・Origin時のCORSヘッダ除去も確認できました。

  ## 差分案

  | 対応 | 変更案・追加試験                                             |
  | ---- | ------------------------------------------------------------ |
  | 1    | 本文受信前に総受付枠を取得。POST応答にも保持数と送信期限を設定。未完本文128本・受信停止した応答・切断時の枠解放を試験 |
  | 2    | LRU追い出し時に関連ID・イベント・時刻情報を一括解放。2001セッション以上を反復して全関連辞書の上限を検査 |
  | 3    | 未回収メッセージの生成時刻を基準にする。空キュー・処理中・回収中のセッションを期限だけで削除しない。120秒境界をまたぐ正常呼び出しを追加 |
  | 4    | 実メッセージのUTF-8直列化バイト数で管理。日本語・絵文字を含む境界試験を追加。「184 KiB」は測定例へ訂正 |
  | 5    | 内部クライアント生成と起動後検査が終わるまで外部MCPを受け付けない。待受開始直後から同時要求し、生成数1・残存heartbeatなしを検査 |
  | 6    | openも絶対パス化。操作別にsrc／dst／dir_fdを解決し、解決不能を検出成功扱いにしない。相対open・FD宛rename・停止時操作の陽性対照を追加 |
  | 7    | 公開queue入口の検証エラーを入力値なしで記録する。正常・不正・例外の各要求についてstdout／stderr／Spacesログへの入力残存を試験し、開示を更新 |
  | 8    | 公開用bind・許可Host・固定ポート・起動検査を仕様化。Spaces環境分岐でS03・M01〜M03・P01を実施 |
  | 9    | 非公開参照と絶対パスを置換指示の文章からも除去。上流不具合は公開用の互換性説明へ要約。開示の承認・署名・対象コミットを確定 |
  | 10   | 空白の右側も`significant()`で探索。強調記号が空白の左右・先頭末尾にある引用を試験 |
  | 11   | 版運用の境界を正本のコミット基準へ統一。E01は20本完了へ同期。Codex②対応表の「解消済み」を今回の残存事項に合わせて訂正 |