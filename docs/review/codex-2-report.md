<!-- Codex 独立検査②の報告本文。著者が配置。施工側でローカルの絶対パスをリポジトリ相対に置換した（17 箇所・2026-09-18）。本文そのものには手を入れていない。 -->

検査コミットSHA：`41fa3f95832d9fc711c79a61d3d9ad5d214e9a39`（`main`）

## 指摘

前回10件の検算結果は以下のとおりです。

| 前回                        | 判定                 | 現物・試験での確認                                           |
| --------------------------- | -------------------- | ------------------------------------------------------------ |
| ① 正本・README・prompts不足 | 修正済み             | SPEC v2.2、README、日英6件を確認。検索順位も同期済み。ただし対応表に①の行がない。 |
| ② 64 KiB迂回                | 修正済み             | TE＋CLは400。実バイト数65,536は通過、65,537は413。受信時間には別の退行あり。 |
| ③ プロキシ環境・起動確認    | 修正済み             | 大小文字のプロキシ変数除去、NO_PROXY固定、起動前後検査を確認。 |
| ④ queue・実行上限           | 一部残存             | `/call/*`遮断、共有4枠、処理待ち16件は実装済み。受付待機・未回収結果は未制限。 |
| ⑤ 無制限キャッシュ・ログ    | 修正済みだが退行あり | キャッシュ512件、ログ256件に制限。ただしキャッシュに並行操作の競合。 |
| ⑥ S03監査                   | 一部残存             | flags・ポート・起動時記録・陽性対照を追加済み。書き換え先・停止処理・自己接続判定に漏れ。 |
| ⑦ 表ハッシュ                | 修正済み             | TERMS・PATTERNSの再計算値、定数、個別規則文書が一致。生成時の定数更新も動作。 |
| ⑧ 孤立サロゲート            | 修正済み             | 10入力位置で`invalid_input`、応答のUTF-8化成功。             |
| ⑨ FIFO                      | 修正済み             | モック試験でopen前の拒否、NONBLOCK／NOFOLLOW、inode交換検出を確認。 |
| ⑩ 文書の旧記述              | 一部残存             | SEARCHの「初版は空」、PROMPTSの承認「日本語のみ」が残る。    |

1. **P2：queueの待機要求・未回収結果が制限されていない。**  
   [app.py:301](<app.py:301>)のセマフォには待機者数の上限がなく、実際のGuardへの32要求で**受付処理8件・待機24件・拒否0件**を再現した。さらに枠はjoin応答直後に解放される。固定Gradioの処理を使ったメモリ内試験では、結果を回収せず32ジョブを完了させると、**処理待ち0件・未回収メッセージ32件・未回収ID32件**になった。[README:253](<README.md:253>)の8件／16件だけでは総保持量を制限できない。

2. **P2：LRUへの修正が並行呼び出しで例外を起こす。**  
   [tools.py:561](<mekiki_reader/tools.py:561>)の取得と`move_to_end()`の間に、別スレッドが対象を追い出せる。512件の状態で実行順を固定すると **`KeyError: 'key0'`** を再現した。通常の並行ツール呼び出しが通信エラーになり得る。

3. **P2：「本文到着10秒」がチャンクごとにリセットされる。**  
   [app.py:286](<app.py:286>)は各`receive()`に個別のtimeoutを設定している。定数を50 msに縮めた試験で、30 ms間隔の3チャンクは**92 ms後に成功**した。少量ずつ送れば総受信時間を延ばせるため、[LIMITS:29](<docs/rules/LIMITS.md:29>)の記述と一致しない。

4. **P2：S03の「起動から停止まで・自己接続だけ」という検査が不完全。**  
   [server_launcher.py:114](<tests/_support/server_launcher.py:114>)はrename等の最初の引数しか記録せず、相対パスも解決しない。合成イベントで、`/tmp/... → data/llms.txt`のrenameと、起動中の相対パスによる書き込みが検査をすり抜けた。  
   また、[221行](<tests/_support/server_launcher.py:221>)で監査を書き出した**後**に`DEMO.close()`するため、停止処理が記録に含まれない。[自己接続判定:78](<tests/_support/server_launcher.py:78>)は、実bindが127.0.0.1でも`::1:<自ポート>`を免除し、別のIPv6サービスを自己接続と判定する。

5. **P3：不正なHost接尾辞を許す。**  
   [app.py:155](<app.py:155>)は括弧の後が`:`か確認しない。`[::1]x80`が許可対象の`[::1]`として受理される。

6. **P3：生成器が規則一覧のハッシュを更新しない。**  
   [build_patterns.py:297](<scripts/build_patterns.py:297>)によるコードの定数更新とPATTERNS文書更新は正常。しかし`docs/rules/README.md`のハッシュは残るため、内容を変えて再生成すると[整合試験:85](<tests/test_tools.py:85>)が失敗する。生成内容をメモリ内で捕捉して確認した。

7. **P2／P3：README・反映記録に実装や検証根拠より強い記述が残る。**

   | 箇所                                                         | 不一致                                                       |
   | ------------------------------------------------------------ | ------------------------------------------------------------ |
   | [README:255](<README.md:255>) | 「ブラウザからは応答を読めない」は過大。CORS許可ヘッダ除去は、直接表示・同一オリジンの読取を禁止しない。 |
   | [README:87](<README.md:87>) | CL「重複不可」に対し、同値重複は固定h11が畳んで受理する。LIMITSの説明は正しい。 |
   | [README:81](<README.md:81>) | 圧縮候補の「一致20件」は結果数ではなく、各結果の一致位置数。407字の入力で99結果を再現。 |
   | [README:192](<README.md:192>) | 橋渡し経由のprompts6件確認済みという記述に対し、保存された検収記録は英語版追加前の3件。6件の再検収はHTTP直結。 |
   | [README:245](<README.md:245>) | 起動から書き込み0件との記述に対し、現S03は起動中の一時領域書き込みを許容している。 |
   | [反映記録:8](<docs/review/codex-1.md:8>) | 「10件すべて差分案どおり反映」は④⑥の残存と一致しない。DECISIONS:203の「原本未受領」も配置済み原本と矛盾。 |

起動表示8行と`.mcp.json`の接続先は静的照合で一致。promptsは6件、既知の制約はCORS追加後の**9項**。READMEには完全な遮断経路一覧がない。SPECのPATTERNS旧版・旧語形数は、判断ログにv2.3で訂正予定と記録されている。

実行確認はツール試験274件通過、R01の3シードで一致。通信・監査・生成器は現物を使ったメモリ内試験で検算し、実サーバの全結合試験は再実行していない。ファイル変更なし。

## 差分案

- **queue**：受付中8件とは別に待機者数を制限する。未回収結果は回収・破棄まで件数／バイト数／期限を管理する。待機超過と、同一セッションで結果を回収しない反復joinを試験に追加する。
- **LRU**：`functools.lru_cache(maxsize=512)`へ置換するか、取得・並べ替え・追加・追い出しを同じロックで囲む。今回の割込み順を固定する回帰試験を追加する。
- **受信期限・Host**：受信ループ全体に一つの10秒期限を設定する。括弧付きHostの接尾辞は空または`:<ポート>`のみ許可する。低速分割送信と`[::1]x80`を試験へ追加する。
- **S03**：書き換え元・先とdir_fdを記録し、相対パスを解決して判定する。自己接続は実際のbind先とポートに限定する。陽性対照フラグを`finally`で戻し、停止処理後に監査を保存する。これらの検出漏れを陽性対照に加える。
- **生成器**：規則一覧も同時更新するか、一覧の重複ハッシュをなくして個別規則文書を参照させる。生成後に文書整合試験まで通す。
- **文書・対応表**：前回①の行を追加し、④⑥を未完了、⑤を退行ありとして記録する。READMEのCORS・CL重複・20件の単位・queue上限・監査範囲を実効範囲に直す。橋渡し6件は再検収まで確認済みと書かない。遮断一覧、SEARCH/PROMPTSの旧記述、原本受領状態も同期する。