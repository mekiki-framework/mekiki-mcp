# 段階0 回答票（確定・2026-09-18）

読み方：Q番号ごとに採用する案を書く。「案のとおり」＝DECISIONS.md の暫定案を採用。検査室が起草し、著者が確認して確定した（Q02・Q17・Q18 は著者回答）。Claude Code は本票に従って各Qの状態を「確定」に更新する（撤回は行を残す）。

## 0. 緊急（施工と無関係に先に）
- `OPENAI_API_KEY` は再発行し、`~/.zshrc` の値を差し替える（著者作業）。DECISIONS.md には値も経緯の詳細も書かず、「下位エージェントがシェル設定を表示し鍵が露出→再発行済み」の一行だけ残す。

## 1. 判断の級（今後の運用。CLAUDE.md v1.1 に反映済み）
- **著者判断（停止して待つ）**：権利・ライセンス・公開範囲・SPEC の改版・外部への送信や投稿・ローカル環境の変更・費用・対応クライアント・開示文。
- **施工判断（提案として記録し続行）**：規則の細部・実装の選択・テスト設計・ファイル構成の追加。段階末に著者が確認し、撤回できる。
- この区分で Q29〜Q40・Q41〜Q61・Q63〜Q70・Q84〜Q88・Q92 は施工判断。以下の回答は検査室が暫定案を点検した結果。

## 2. 版同定・取得
- Q01 **A**（6748061 を正。台帳の 3.5.0 公開日 9/6 と一致）
- Q02 **B**。著者回答：Release は著者が手動で作成・編集しており、日付の食い違いはありうる。事実として記録し、版同定には使わない
- Q03 後者（commit＋tree＋blob＋SHA-256）＋ `EXPECTED_BUNDLE_SHA256` 定数。脅威想定は**事故検出**（同期による改行変換・誤コピー・部分更新・データと manifest の同時改変）まで。攻撃者モデルは置かず、README でも改竄耐性は主張しない
- Q04 A を主経路、D を独立照合。生成スクリプトは `scripts/build_bundle.py` として保存（再現のため。SPEC v2.1 で構成に追加）
- Q09 追加する／Q10 一律拒否。`.gitignore` に `.DS_Store` を追加／Q11 そのとおり／Q12 そのとおり

## 3. ライセンス（前提：著者＝権利者）
- Q83 **はい**。T1〜T5 とコーパスの権利者は著者本人。以後は権利者の選択として扱う
- Q06 **C**（`data/LICENSE` をバイト一致で同梱＋README/NOTICE に URI。許可一覧に追加＝SPEC v2.1）
- Q07 **同梱する**（帰属文の書式が唯一そこにあり、同梱範囲外への相対リンクも解消。許可一覧に追加）
- Q08 外したまま／Q16 加えない／Q19 含める
- Q13 `data/`＝CC BY 4.0（コーパスのまま）、コード＝MIT。表示は README の「ライセンス」節＋ルートに `NOTICE`（数行）
- Q14 帰属文は CITATION.md L21 の書式。改変の表示は「原文の行は改変しない。行単位で抜粋し JSON に構造化して返す」
- Q15 `mit`（YAML は単一値なので、`data/` の条件は README で明記）
- Q17 著者回答：MIT の著作権者はコーパスの著作者表示と同じ個人名にする（LICENSE の1行目を書き換える）
- Q18 著者回答：T1〜T5 はすべて CC BY 4.0（OSF の登録と一致）。T1 L359 の "CC BY-NC 4.0" は原文中の古い記述で、同梱物のライセンスではない。原文は改変せず、README のライセンス節に「同梱物は data/LICENSE（CC BY 4.0）に従う。論文本文中に別の表記があっても同梱物には及ばない」を一行。コーパス側での注記は著者の別作業（Reader の範囲外）

## 4. 言語
- Q21 **A**（既定 None＝原文。`en` は T4 英訳のみ。不一致は invalid_input。SPEC v2.1）
- Q22 A（固定表 LANG・版つき。ガイド類は en）／Q24 論文＋英訳（案のとおり）／Q61 二つ目と三つ目

## 5. 凍結定式・照合・正規化
- Q23 **A**（T5 の protected sentence 二文。コーパスの validate_corpus.py が保護する二文と一致。SPEC v2.1 の T07 に明記）
- Q41 A2＋行単位／Q42 B2′＋半角カナ明示表・NFKC 不使用／Q43 c-ii＋数字保護（案のとおり）／Q44 d-i／Q45 全角形も含め一切変換しない
- Q46 初版は SPEC のまま／Q47 採用（入力側の NFC 合成のみ・規則 ID `NFC-IN`・`unicodedata.normalize('NFC')` を明示の例外として許可し理由を記録）
- Q48 案のとおり。句読点だけが違う箇所は candidates に出す（T10）／Q49 意味つき版＋表の SHA-256
- Q84 最小長と上限の両方。超過は invalid_input とし limitations に `total=n`（部分結果は返さない）。値は施工段階2で実測して提案
- Q92 案のとおり（規則 CAND）

## 6. スキーマ・ツール
- Q25 後者の運用（英訳の結果に manifest の preparation と authority を常に添える。コーパス側の記述を Reader が格上げしない）
- Q26 別の結果に分ける。マーカー行は含める／Q27 sourceLine で決める。枠部分は別 ID で明示要求時のみ、由来を常に添える／Q28 前者
- Q29 固定の列挙（規則 GUIDE）／Q30 前者／Q32 ①〜④／Q33 前者／Q34 直下の子だけ・heading_only の定義そのとおり／Q35 案のとおり／Q36 前者
- Q31 逐語返却・区分欄なし。SPEC §2.8「四つの身分」は誤記で、意図は「区分に統合しない」。v2.1 で「区分」に直す
- Q37 そのとおり（起草＝検査室、承認＝著者、source_excerpt も承認対象、T11 は fixtures から注入）
- Q38 ①〜⑥・⑧〜⑩案のとおり。⑦は表を起草して承認（SPEC v2.1 で payload・templates・derivative_of を §4 に追加）
- Q39・Q40 そのとおり／Q63 JSON 文字列／Q68 混在／Q70 そのとおり／Q88 案のとおり

## 7. 検索
- Q50 d／Q51 後者／Q54 一つ目／Q55 最後の案／Q57 行／Q58 論文原文（参考文献含む）／Q59 B／Q60 案のとおり
- Q52 抽出候補を著者が承認・初版は空・置き場は `mekiki_reader/terms.py`（v2.1 で構成に追加）
- Q53 match_via 必須。範囲はコーパスが自ら示す対訳と表記揺れに限る（概念対応は入れない＝§11）
- Q56 そのとおり。停止語表は置かない（初版）

## 8. Gradio・環境
- Q62 6.27.0・`run_history=False` 必須。施工段階3着手時に PyPI を取り直して再比較
- Q64 そのまま使って M02 で確認。**番兵関数は入れる**（誤照合時に例外で止める方が安全）。上流への報告は著者判断（配置段階二の前）
- Q65 queue=False＋セマフォ／Q67 そのとおり／Q69 受け入れる／Q85 A／Q86 そのとおり／Q87 そのとおり／Q93 案のとおり
- Q66 案のとおり。遮断できなければ停止して報告。⑥は再現と報告の状況が決まるまで README に書かない
- Q71 配置段階一＝Claude Code・Claude Desktop。ChatGPT は配置段階二／Q73 配置段階二まで待つ／Q91 案のとおり
- Q72 許可（版固定・記録）。ただし先に Desktop の HTTP 直結の可否を確かめ、直結できるなら使わない
- Q74 **3.13**／Q75 uv 経由を許可（`brew install uv` → `uv python install 3.13`）／Q76 施工段階1で `.venv` と pytest を許可。固定は推移依存までハッシュ付き（uv pip compile）。`requirements-dev.txt` 新設可
- Q77 配置段階二の前に実測／Q78 minor を引用符付き（`"3.13"`）／Q79 明示（`.venv/bin/python app.py`）／Q80 起動手順で外す／Q81 `docs/acceptance/<日付>-<クライアント>.md`

## 9. 開示・検収・E01
- Q20 実測後に起草／Q89 そのとおり／Q90 両方（Paper2Agent＝型の出典、T1〜T5＝読解対象）
- Q82 ja／en 両方。クライアントは Claude Code（配置段階一）。ガイド条件は「なし」と「read_with_guards」の二条件。AI_READING_TESTS の resource は読ませない（読んだ場合は記録）。判定は SPEC §7 の文言に限り、コーパス基準との照合は記録のみ
- Q95 ①公開前に、非公開資料への参照を「方針書 v2 冒頭の改版行」に置換 ②「配置段階二」に統一 ③Q64・Q66⑥の行番号つき分析は、公開前に要約化（未報告の脆弱性疑いの詳細は出さない）

## 10. 段階0の範囲と手続き
- Q94 **追認**（一時領域・非同梱・未コミット）。ただし CLAUDE.md v1.1 の規則10（リポジトリ外の読取禁止）を以後の条件とする
- Q05 SPEC を **v2.1** に改版する（検査室が起草・著者承認）。それまで施工は本票の回答を仮確定として進めてよい

## 11. SPEC v2.1 改版項目（検査室起草予定）
1. §3 許可一覧に `data/LICENSE`・`data/CITATION.md` を追加
2. §3 構成に `scripts/build_bundle.py`・`mekiki_reader/terms.py`・`tests/conftest.py`・`tests/_support/`・`tests/fixtures/`・`docs/`（PLAN・rules・acceptance・stage0_facts）・`requirements-dev.txt`・`NOTICE` を追加
3. §5 `language` の既定を None（原文）に。`en` は T4 のみ。不一致は invalid_input
4. §2.8 「四つの身分」→「区分」
5. §4 に payload・templates・derivative_of を追加（Q38）
6. §7 T07 の二文を明記（T5 abstract／§5.4）
7. §9 一式に LICENSE・NOTICE・CITATION を追加
8. §2.10 処理時間上限の扱い（Q85 A）、§7 S03 の宣言範囲（Q86）
9. §8 対応クライアントの範囲と段階（Q71・Q91）

## 12. コミットと段階1の入口
- DECISIONS.md を今の内容でコミット（`Stage 0: plan and open questions`）
- 段階1の最初の作業：付録A→`docs/PLAN.md`、付録B→`docs/rules/`（版つき規則表）、付録C→`docs/stage0_facts.md` に分離し、DECISIONS.md はログ表のみにする。本票を反映して各Qの状態を更新
- 段階1本体：Q03・Q04 の経路で `data/` 取得→`bundle_manifest.json`→`corpus.py`→D01〜D04。終わったら止まる
